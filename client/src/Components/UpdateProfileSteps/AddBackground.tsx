import React from "react";

interface Props {
  background: any;
  setBackgroundPhoto: (background: any) => void;
  setPage: () => void;
}
const AddBackground: React.FC<Props> = () => {
  return <div>AddBackground</div>;
};

export default AddBackground;
